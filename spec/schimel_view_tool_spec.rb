require "spec_helper"

describe SchimelViewTool do
  it "has a version number" do
    expect(SchimelViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
